services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ordering-front
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped
